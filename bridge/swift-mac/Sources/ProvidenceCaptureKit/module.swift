// ProvidenceCaptureKit - shared capture primitives.
//
// In Phase 1 this library is a placeholder so the SPM product exists and the
// overlay app (providence-overlay) can link it as a dependency later. Real
// capture / AX code moves here in Phase 6+ so the bridge binary and the
// overlay app share a single ScreenCaptureKit / AX implementation.

public enum ProvidenceCaptureKit {
    public static let version = "0.1.0-phase1"
}
